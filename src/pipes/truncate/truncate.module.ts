import {NgModule} from "@angular/core";
import {truncate} from "./truncate";

@NgModule({
    declarations: [
        truncate,
    ],
    imports: [
    ],
    exports: [
        truncate
    ]
})
export class TruncateModule {}
